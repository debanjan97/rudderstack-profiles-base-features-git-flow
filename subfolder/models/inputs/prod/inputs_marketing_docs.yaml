inputs:
  - name: docs_pages
    contract:
      is_event_stream: true
    app_defaults:
      table: RUDDER_AUTOTRACK_DATA.AUTOTRACK.PAGES
      occurred_at_col: timestamp
      ids:
        - select: anonymous_id
          type: anon_id
          entity: user